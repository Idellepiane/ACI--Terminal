# Entorno virtual y cachés
.venv/
__pycache__/
*.pyc

# Secretos — NUNCA subir la API key al repo
.streamlit/secrets.toml

# Watchlist persistida (es local de cada máquina/usuario)
.watchlist.json

# Entregables pesados / generados (opcional)
*.zip

# Sistema
.DS_Store
